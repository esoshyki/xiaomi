import { Container } from "../ui/Container";
import ThemeButtons from "./Buttons";

const Theme = () => {
    return (
        <Container fullWidth >
            <ThemeButtons />
        </Container>
    );
};

export default Theme;
