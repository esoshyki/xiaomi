import { DefaultTheme } from "styled-components";

const defaultTheme : DefaultTheme = {
    borderRadius: "5px",

    colors: {
        main: "#000",
        secondary: "megenta",
        bgMain: "#2f2f2f"
    },

    spaces: {
        0: "0",
        1: "1rem"
    }
}

export { defaultTheme }