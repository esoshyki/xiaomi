type ButtonProps = {
    bgcolor: string
    textColor: string
    borderColor: string
    hoverBackground: string
    activeBackground: string
}

export type ButtonTheme = {
    primary: ButtonProps
    secondary: ButtonProps
    outline: ButtonProps
}